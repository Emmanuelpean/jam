## TODO

### TOAST
* [ ] If no currency is found, use the default currency

### Select Widgets
* [X] Display the entry associated with the hovered option

### Job Modal
* [X] Add option to select the source (recruiter, aggregator, other)

### Advanced Dashboard
* [ ] Add a "active jobs" log showing jobs with the most activity
* [X] Add a "favourite" column to the job table

### Tables
* [X] Move the scrollbar inside the tables so that the header is always visible
* [ ] Add the ability to filter by columns and hide rejected jobs

* [X] Add favourite filters for scraping

* [X] On Small screens, modal elements on multiple columns and rows are all on same column but spacing differs
* [X] In the frontend, when sending the version email, when counting the number of users, it should ignore non-verified users

* [X] Do not rate jobs with past deadline or closed and skip them